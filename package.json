{
  "name": "root",
  "private": true,
  "scripts": {
    "lint": "lerna run lint --stream",
    "flow": "flow",
    "test": "lerna run test --stream",
    "clean": "lerna run clean --stream"
  },
  "workspaces": [
    "packages/*"
  ],
  "devDependencies": {
    "flow-bin": "0.83.0",
    "flow-typed": "2.5.1",
    "lerna": "3.4.3"
  }
}
